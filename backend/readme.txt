Backend setup:
1) cd backend
2) npm install
3) (optional) set env SEED_DB=true to seed services
4) npm run dev

Env:
- MONGODB_URI (default mongodb://127.0.0.1:27017/glitter-glaze)
- PORT (default 5000)
- SEED_DB=true to seed services at startup

